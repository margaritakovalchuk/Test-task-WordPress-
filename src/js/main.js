window.onload = init;

function init() {
    const sliderTeam = new Slider('.slider-team');
    sliderTeam.run();

    const sliderComment= new Slider('.slider-comment');
    sliderComment.run();
}